const { Contact, modelContactSchemas } = require("./contacts");
const { User, modelUserSchemas } = require("./user");

module.exports = {
  Contact,
  modelContactSchemas,
  User,
  modelUserSchemas,
};
